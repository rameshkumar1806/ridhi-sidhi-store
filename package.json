{
  "name": "ridhi-sidhi-store",
  "version": "1.0.0",
  "description": "Root package manager for Ridhi Sidhi General Store",
  "private": true,
  "scripts": {
    "install:server": "npm install --prefix server",
    "install:client": "npm install --prefix client --include=dev",
    "build:client": "npm run build --prefix client",
    "build": "npm run install:server && npm run install:client && npm run build:client",
    "start": "npm start --prefix server",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "dev": "npx concurrently \"npm run server\" \"npm run client\""
  }
}
